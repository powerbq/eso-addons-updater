.pragma library

// Backgrounds
var bg         = "#1e1e1e"   // main window, overlays
var bgSurface  = "#252525"   // bars, toolbars
var bgSelected = "#2d2d2d"   // selected list item

// Borders and separators
var separator   = "#3a3a3a"
var inputBorder = "#cccccc"

// Accent
var accent     = "#0078d4"
var accentText = "#ffffff"

// Text
var textPrimary   = "#e5e5e5"
var textSecondary = "#888888"
var textMuted     = "#666666"
var textLog       = "#cccccc"

// Chips
var chipBorder = "#555555"
var chipText   = "#aaaaaa"

// Inputs
var inputBg   = "#d0d0d0"
var inputText = "#111111"

// Special
var starActive   = "#f0b429"
var starInactive = "#555555"

// Opacity
var disabledOpacity = 0.35

// Component heights
var barHeight     = 36
var toolbarHeight = 48
var chipHeight    = 24
var buttonHeight  = 32

// Font sizes (sizes used across multiple files)
var fontSm   = 11   // stat labels, bar labels
var fontMd   = 12   // toolbar secondary text
var fontBase = 13   // list item text, log, detail text
var fontLg   = 14   // empty state, tab buttons
